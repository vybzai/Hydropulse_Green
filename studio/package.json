{
  "name": "hydropulse-studio",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "seed:build": "node scripts/build-seed.mjs",
    "seed": "npm run seed:build && sanity dataset import seed/seed.ndjson production --replace"
  },
  "dependencies": {
    "@sanity/vision": "^3.99.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sanity": "^3.99.0",
    "styled-components": "^6.1.15"
  }
}
