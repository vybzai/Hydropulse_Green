# Copy to .env and fill in from https://www.sanity.io/manage
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Optional: for CI / headless CLI (dataset import, deploy). Create a token with Editor rights at
# https://www.sanity.io/manage → Project → API → Tokens
# SANITY_AUTH_TOKEN=
